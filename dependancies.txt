*pyinstaller

*pywin32 - install via wheel pywin32-219-cp26-none-win32.whl (RESET)

*websocket-client

*zbarlib - use instructions here basically you need python 2.6 32 bit, the windows installer for zbar, and the windows installer for the python module. For that reason, the virtualenv must be made with --system-site-packages

*Pillow

then follow instructions here: http://stackoverflow.com/questions/7775339/python26-win32-zbar-importerror-dll-load-failed

RESET cmd so path can take effect

*SimpleCV - makes life easier to access camera and decode barcode

*pygame, scipy, numpy, opencv - needed by simplecv to open camera

*spooky

*pycrypto

*keyring

*pymongo

sudo apt-get install build-essential #MAKE AND GCC
sudo apt-get install python-gevent OR pip install gevent # AFTER apt-get python-dev
sudo apt-get install python-pip #outdated
sudo pip install -U pip #to get rid of assertion error when using pip freeze
sudo apt-get install python-pyside
sudo apt-get install gcc #already installed on 15.04
sudo apt-get install python-dev #Python.h: No such file or directory
sudo apt-get install -y mongodb #-y means yes to all

####
development environment

get Lubuntu 64 bit ISO
make sure Virtualization Technique or VT is enabled in system bios
install virtualbox
install lubuntu
boot it up
sudo apt-get install build-essential
mount the vbox-guest-addition iso from the virtualbox menu
sudo apt-get install virtualenv
cd to a folder where you want a virtualenv
virtualenv --system-site-packages python-2.7-system
pip install -U pip #to fix pip freeze issue
sudo apt-get install python-dev